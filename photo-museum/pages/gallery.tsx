import Layout, {Page} from "../components/Layout";

const Gallery = () => {
    return (
        <Layout currentPage={Page.GALLERY}>
            <div>
                <p>Gallery</p>
            </div>
        </Layout>
    )
};

export default Gallery;
