import Layout, {Page} from "../components/Layout";

const upload = () => {
    return (
        <Layout currentPage={Page.UPLOAD}>
            <p>Upload</p>
        </Layout>
    );
};

export default upload;
